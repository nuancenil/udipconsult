source "https://rubygems.org"

# 用 GitHub Pages 維護的整合套件（內含 jekyll、seo-tag、feed 等白名單外掛）
gem "github-pages", group: :jekyll_plugins

# Ruby 3 需要 webrick 才能本機 serve（Actions 不一定用到，但本機會用到）
gem "webrick", "~> 1.8"
